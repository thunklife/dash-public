DashJS Captions Prototype
=========================

This is just to test out DASH-MPG content with streaming TTML captions

Installation
============

	λ git clone
	λ cd dash-public
	λ npm install
	λ npm run serve

if ```npm run serve``` throws:

	λ npm install -g http-server
	λ http-server

Should be good to go.
